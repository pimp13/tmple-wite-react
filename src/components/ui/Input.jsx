export const Input = ({ props }) => {
  return (
    <input
      className="border border-gray-300 rounded-md px-3 py-1 focus:border-blue-500 focus:outline-none transition-all"
      {...props}
    />
  );
};
